mutation UPDATE_COMPANY_MANAGER($id: ID!, $data: UpdateCompanyManagerInput!) {
    updateCompanyManager(id: $id, data: $data) {
        id
        company_id
        manager_id
        has_seen
    }
}
